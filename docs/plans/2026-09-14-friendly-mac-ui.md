# 原生 Mac 界面调整实施计划

目标：落实已确认的设计，保留全部业务功能。分支：`codex/friendly-mac-ui`。

约束：只调整 SwiftUI 展示和中英文文案；不改账号服务、推荐、排序和统计算法；不改图标；不生成 DMG。直接在本任务执行，不使用 Superpowers。

## 1. 账号页和基础样式

- [x] 新增 `AppStyle.swift`，统一深浅色背景、蓝色操作与边线。
- [x] `ContentView.swift` 取消全局强制大字和大控件，入口仍为 `DashboardView()`。
- [x] `DashboardView.swift` 将账号记录变为上下两层；保留现有 `refreshAction`、`switchAction`、`aliasAction`、`removeAction`、`reauthenticateAction` 和拖动回调。
- [x] 顶部摘要以当前和推荐账号分列，主按钮靠近目标。分组开关与管理合并到菜单，换组保留在账号菜单。
- [x] 失效账号使用 `account.authInvalid` 标记历史值，不能修改其额度或切换规则。

## 2. 统计、记录与反馈

- [x] 统计总量优先，所有分项和四位金额保持可见或可展开。账号周期明细适应窄窗口。
- [x] `UsageLearningView.swift` 将图表提前、说明折叠；日期范围、数据限制和全部观察值保留。
- [x] 切换记录按 `Calendar.current.startOfDay(for: record.timestamp)` 分组；日期、秒级时间、失败原因完整保留。
- [x] 设置、登录和错误提示统一字号与间距；维持原确认和取消回调。
- [x] `Localization.swift` 补齐新增中文键的英文翻译。

## 3. 验证与交付

- [x] 运行 `scripts/build-app.sh`，编译并替换 `dist/Codex Switch5.app`（脚本保留旧包备份）。
- [x] 只启动此包检查账号、统计、记录及菜单；不执行真实账号切换、移除或登录。
- [x] 实测约 1006 像素窗口的中英文显示，设置已恢复中文。窄窗口拖动检查因 UI 工具返回 noWindowsAvailable 未完成；深色、760/1440 宽度及全键盘操作未实测。
- [x] 检查 `git diff --check`、新增翻译重复键和业务代码未改动；报告实际增删行数及未测项。
- [x] 本地提交源码与设计文档，不推送远端。

## 验证结果

- 最终 release 构建和应用签名成功，已重启 `dist/Codex Switch5.app`。未生成 DMG。
- 实际查看账号、统计摘要、账号周期明细、使用习惯、日期分组记录；确认 Token 明细可展开，账号更多菜单保留别名、换组、身份和移除。
- 英文检查发现并修正今日翻译缺失、设置行拥挤；重新打包并查看英文设置后恢复中文。
- `git diff --check` 通过，翻译字典无重复键；Core 与 AppModel 无改动。
- 未进行真实切换、登录、移除或更换分组；不宣称这些业务流程经过本轮端到端测试。
- 深色和最小窗口的视觉验收仍需补查，当前完成的是代码实现与上述范围内的实机检查。

## 第二轮反馈修正

- 摘要移入账号滚动区并缩小，固定工具栏约由 245 缩至 85 像素（1006 像素宽窗口截图）。
- 左右工具区等宽，使三项导航按窗口中心排列；窄窗口沿用双行布局。
- 拖动不再因组合排序被禁用；首次拖动采用当前显示顺序并切换至手动排序，分组显示时只调整同组顺序，不修改归属。把手扩大至 20×32。
- Token 数值紧跟周期标题，周期块增加浅色竖分隔。
- release 编译通过，实机截图确认顶部、导航和明细。自动化拖动及滚动调用返回 noWindowsAvailable，鼠标拖动与滚动未能实测。
- 本次新增 45 行、删除 27 行源码。未修改账号服务和 Token 计算。

### 第三轮：区域关系与明细密度

- 当前账号和推荐账号置于独立浅底区域，中间用箭头表达切换方向；与所有账号工具栏之间增加间距和分隔线。
- 更新时间并入账号工具栏。统计和记录页仅固定精简操作栏；本机记录提示及统计说明放入滚动内容，取消底部固定说明。
- 账号明细由五个网格块改为五行对照表，共享输入、缓存、输出、推理、自动审查与金额列标题，保留周期时间和预测数据。
- 最新 release 构建通过，重复翻译条目已清理；重新启动 dist 应用并通过窗口截图核对账号、账号明细和切换记录三页。未执行真实账号切换。拖动和滚动手势此前受界面工具限制，不能声称完成手势验证。
- 本轮修改仍保存在工作区；上一轮 git commit 被自动审批以工作区额度不足拒绝，未绕过。

### 第四轮：说明位置与居中摘要

- 使用习惯的时间范围选择移到右侧，与左侧日期范围同一行；去除持续校准提示。
- 数据来源与统计说明统一放到底部固定小字区域，取消顶部说明及其展开控件。
- 当前账号与推荐账号分别居中，切换按钮位于两者之间；保留摘要随页面滚动的行为。
- release 构建与差异空白检查通过；重启 dist 应用，截图核对账号、使用习惯、账号明细。仅调整布局，未操作真实账号切换。

### 第五轮：明细联动与列宽

- 放大账号摘要中间的切换按钮；三张统计卡共用展开状态，展开或收起任意一张同步影响全部。
- 开始与结束时间改为同一行；各数据列共同分配剩余宽度，避免第一列独占空白；移除末行底部分隔线。
- release 构建与差异检查通过，最新应用截图确认布局；实际点击第一张展开、第二张收起，三个明细状态均同步。

### 第六轮：明细点击与说明清理

- Token 明细改为整行 Button，文字、箭头和行内空白共用点击区域，保留三个卡片同步展开。
- 摘要切换按钮改为浅蓝底、蓝字与细边框，提供按下和禁用状态。
- 柱形说明移至使用习惯图表下方并左对齐；删除旧记录说明展开区及等待、休息、安排切换相关说明。保留并重写当前每小时额度数值所需的计算说明，放在额度数据下方。
- release 构建、差异检查通过；最新应用核对浅蓝按钮、明细展开及说明位置。按钮通过辅助功能点击验证，未单独验证鼠标坐标点击。

### 第七轮：Token 明细主次

- 输入、输出采用较大半粗字体；缓存、推理分别缩进置于对应主项下，使用小号次要文字。
- 自动审查独立放在细分隔线下，以小字显示，保留不计入总数的说明。
- release 构建及差异检查通过；最新应用实际展开三张卡并截图核对字体、缩进和分组，数据与联动行为保留。

### 第八轮：自动审查标签与重置时间

- 移除自动审查前的分隔线，说明合并为“自动审查（未计入 Token 总数）”，数值仍在同行。
- 5h 周期时间文案由结束改为重置，复用已有中英文翻译，未改变时间计算。
- release 构建及差异检查通过；本轮未重新进行窗口截图验证。

### 窄窗口顶部与图表拖动

- 顶部名称、页面切换、操作保持同一行；760 宽度重启截图确认。
- 历史图表隐藏横向滚动条，通过原生左键平移手势改变横向滚动位置，并限制在内容边界内。
- release 构建及差异检查通过；鼠标拖动验证被界面工具 noWindowsAvailable 阻断，实际手势尚未验证。

### 统计页调整窗口宽度的性能修复

- 三张统计卡与历史图表的数据在记录、账号、语言、统计时段变化及进入统计页时生成，布局直接使用结果，避免窗口每次变化重复扫描历史记录。
- 宽度状态改为窄屏与宽屏布尔值，仅跨越分界时更新，避免逐像素触发额外刷新。
- release 构建与差异检查通过，重启应用后辅助功能确认三张卡及历史图表正常显示。未获得拖动窗口的性能测量，不能声称已验证帧率或完全消除卡顿。

## 启动兼容修复

- 启动从本地登录凭据识别账号，额度缓存缺失时也显示账号；未知额度显示“—”，启动后查询额度。
- 保留数据重装继续使用原名称和缓存；凭据已删除的旧账号不会仅凭缓存重新出现。
- 没有活动凭据、仅剩账号存档时，允许复制存档恢复；已有未知活动文件时禁止覆盖。
- 核对账号身份后匹配存档，忽略备份、无效文件，并处理账号名称冲突。
- 隔离临时目录检查涵盖首次启动、重装、缺缓存、同名账号、无效文件和恢复防覆盖；不修改真实账号用于测试。
- 本次替换旧的重复识别逻辑，新增可单独验证的识别组件；没有删除用户数据文件。
